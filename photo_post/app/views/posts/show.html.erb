<h1><%= @post.title %></h1>

<% if @post.image.attached? %>
  <%= image_tag @post.image.variant(resize_to_fill: [600, 600]) %>
<% end %>

<p><%= @post.content %></p>
<%= link_to "一覧へ戻る", posts_path %>
