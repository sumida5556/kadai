<h2>マイページ</h2>
<p>名前: <%= @user.name %></p>
<p>自己紹介: <%= simple_format(@user.content) %></p>

<%= link_to "プロフィール編集", edit_mypage_path %>

<h3>あなたの投稿</h3>
<% if @posts.any? %>
  <ul>
    <% @posts.each do |post| %>
      <li><%= link_to post.title, post_path(post) %> （<%= post.created_at.strftime("%Y/%m/%d %H:%M") %>）</li>
    <% end %>
  </ul>
<% else %>
  <p>投稿がありません</p>
<% end %>
